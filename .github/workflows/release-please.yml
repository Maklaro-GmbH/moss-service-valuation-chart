on:
  push:
    branches:
      - main
name: release-please
permissions:
  contents: write
  issues: write
  pull-requests: write
jobs:
  release-please:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v4.3
        id: release
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
